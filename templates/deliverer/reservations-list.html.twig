<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>{{ deliverer.getName() }}</title>
</head>
<body>
    <h1>{{ deliverer.getName() }}</h1>
    
    <table class="w-50 table table-striped table-hover text-center">
        <thead>
            <tr>
                <th>Reservation ID</th>
                <th>Vehicle</th>
            </tr>
        </thead>
        <tbody>
            {% for reservation in deliverer.getReservations() %}
            <tr>
                <td> {{ reservation.id }} </td>
                <td> {{ reservation.getVehicle().brand }} </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="2"> No reservations for this deliverer </td>
            </tr>
            {% endfor %}
        </tbody>

    </table>
    <a style="margin-left: 15px" class="btn btn-outline-primary" href="{{ path('show_all_deliverers') }}">Go back</a>

</body>
</html>