<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Reservations</title>
</head>
<body>
    <h1>Reservations</h1>   

    <table class="table table-striped table-hover text-center">
        <thead>
            <tr>
                <th>Id</th>
                <th>Deliverer</th>
                <th>Vehicle </th>
            </tr>
        </thead>
        <tbody>
            {% for reservation in reservations %}
                <tr>
                    <td>{{ reservation.getId() }}</td>
                    <td>{{ reservation.getDeliverer().getName() }}</td>
                    <td>{{ reservation.getVehicle().getBrand() }}</td>
                </tr>
            {% else %}
                <tr>
                    <td class="text-center">No deliverers found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <a style="margin-left: 15px" class="btn btn-outline-primary" href="{{ path('start_page') }}" > Go home </a>
</body>
</html>